# EzMicroservices

???????????? .NET 8 ?????????
- ?????????OpenIddict + ASP.NET Core Identity + PostgreSQL
- API Gateway?YARP
- BFF??? Orders / Inventory
- ?????Orders?Inventory?Payments
- ???????PostgreSQL?RabbitMQ?Elasticsearch?ELK?Nacos
- ?????????????Nacos?MQ??????? UoW

## ????
- `src/Gateways/ApiGateway`??????JWT ???????
- `src/Bff/WebBff`??????
- `src/Services/AuthCenter/AuthCenter.Api`????? / OIDC / ??????
- `src/Services/Orders/*`?????????? RabbitMQ ??? Elasticsearch ??
- `src/Services/Inventory/*`??????
- `src/Services/Payments/*`?????????? RabbitMQ ????????
- `src/BuildingBlocks.*`?????
- `deploy/helm`?Kubernetes / Helm ????
- `docker-compose.yml`?????????

## ????
- AuthCenter: `http://localhost:5085`
- API Gateway: `http://localhost:5096`
- Web BFF: `http://localhost:5097`
- Orders API: `http://localhost:5239`
- Inventory API: `http://localhost:5270`
- Payments API: `http://localhost:5066`

## ??????
`docker-compose.yml` ????
- PostgreSQL 16
- RabbitMQ 4.1 Management
- Elasticsearch 9.3
- Kibana 9.3
- Logstash 9.3
- Filebeat 9.3
- Nacos 2.5 standalone

### ????
- RabbitMQ ????`http://localhost:15672`?`guest / guest`?
- Kibana?`http://localhost:5601`
- Elasticsearch?`http://localhost:9200`
- Nacos?`http://localhost:8848/nacos`

## ????
### 1. ??????
```powershell
docker compose up -d
```

### 2. ?????
```powershell
dotnet restore .\EzMicroservices.sln --configfile .\NuGet.Config
dotnet build .\EzMicroservices.sln --configfile .\NuGet.Config
```

### 3. ????
????????
```powershell
dotnet run --project .\src\Services\AuthCenter\AuthCenter.Api --launch-profile http
dotnet run --project .\src\Services\Orders\Orders.Api --launch-profile http
dotnet run --project .\src\Services\Inventory\Inventory.Api --launch-profile http
dotnet run --project .\src\Services\Payments\Payments.Api --launch-profile http
dotnet run --project .\src\Bff\WebBff --launch-profile http
dotnet run --project .\src\Gateways\ApiGateway --launch-profile http
```

> ??? `launchSettings.json` ????????? `Nacos__RegisterService=true`?Nacos ?????????

## ????
### ?????
- ????`admin`
- ???`Admin123!`

### ?????
- `eztrade.cli`??? `password`?`refresh_token`
- `eztrade.service`??? `client_credentials`

### ??????
```powershell
$body = "grant_type=password&client_id=eztrade.cli&username=admin&password=Admin123!&scope=openid profile email roles offline_access orders inventory payments identity"
Invoke-RestMethod -Method Post -Uri "http://localhost:5085/connect/token" -ContentType "application/x-www-form-urlencoded" -Body $body
```

## ????
### ?? -> ??????
1. ?? Orders ????
2. Orders ????? PostgreSQL
3. Orders ?? `orders.created` ? RabbitMQ
4. Payments ?????????/??????
5. Orders ???? Elasticsearch?????

### ???????
```powershell
$token = "<access_token>"
$headers = @{ Authorization = "Bearer $token" }
$body = @{
  customerId = "11111111-1111-1111-1111-111111111111"
  lines = @(
    @{
      productId = "22222222-2222-2222-2222-222222222222"
      quantity = 2
      unitPrice = 199.00
    }
  )
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:5096/orders" -Headers $headers -ContentType "application/json" -Body $body
```

### ?????????
```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:5096/payments/<orderId>" -Headers $headers
```

### ???????
```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:5096/orders/search?keyword=11111111-1111-1111-1111-111111111111" -Headers $headers
```

## ?????
- ??????? Serilog + ECS JSON ????
- ?????????????? `logs` ???
- Filebeat ??? `src/**/logs/*.json`
- ?????`Filebeat -> Logstash -> Elasticsearch -> Kibana`

## Nacos
- ??? JSON ????
- ??????? / ??
- ?????
  - `LoadConfiguration = false`
  - `RegisterService = false`?`launchSettings.json` ????????? `true`?
- Kubernetes / ??????? Helm values ???????????

## Helm ??
????????
- `ASPNETCORE_URLS`
- Authentication ??
- ElasticLogging ??
- Nacos ??
- RabbitMQ ???Orders / Payments?
- Elasticsearch ???Orders?

???
```powershell
helm upgrade --install orders .\deploy\helm\orders `
  --set image.repository=myregistry/orders `
  --set image.tag=dev `
  --set connectionString="Host=postgres;Port=5432;Database=ordersdb;Username=postgres;Password=postgres"
```

## ?????????????
- PostgreSQL + EF Core ??
- JWT / OIDC ????????
- ???????
- Serilog + ECS + ELK ????
- RabbitMQ ?????? / ??
- Elasticsearch ????
- Nacos ???? / ???????
- ???????????
- Helm ??????

## ???????????
1. ? `EnsureCreated()` ??????? `EF Core Migrations`
2. ? Orders ????????? Outbox????????????????????
3. ?????????????????? Secret / Vault
4. ?? OpenTelemetry traces / metrics?? ELK ??????????
5. ???????`orders.created -> payments` ?????Elasticsearch ???Nacos ??
