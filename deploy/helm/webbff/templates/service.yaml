apiVersion: v1
kind: Service
metadata:
  name: {{ include "webbff.fullname" . }}
  labels: {{- include "webbff.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector: {{- include "webbff.selectorLabels" . | nindent 4 }}
